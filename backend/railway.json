{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m gunicorn -w 4 -b 0.0.0.0:$PORT wsgi:app",
    "healthcheckPath": "/api/tasks",
    "restartPolicyType": "ON_FAILURE"
  }
}
